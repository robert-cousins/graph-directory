{
  "name": "@graph-directory/core-ingestion",
  "version": "1.0.0",
  "description": "Graph Directory Ingestion Plane - Safe, isolated data ingestion pipeline",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "eslint src/**/*.ts",
    "test": "jest --testPathPattern=core-ingestion"
  },
  "dependencies": {
    "@graph-directory/core-mutators": "workspace:*",
    "@graph-directory/core-contracts": "workspace:*",
    "@supabase/supabase-js": "^2.94.0",
    "zod": "^4.3.6",
    "crypto": "^1.0.1"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2"
  }
}
